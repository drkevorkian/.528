[package]
name = "libsrs_video"
version = "0.1.0"
edition = "2021"

[dependencies]
crc32fast = "1"
libsrs_bitio = { path = "../libsrs_bitio" }
thiserror = "2"

[features]
default = []
gpu-wgpu = []
gpu-cuda = []
